# UI 框架

|React|VUE|Svelte| ... |
|----|----|----|----|

SACSS 核心是 CSS，理论上支持所有支持 CSS 的项目。

使用方式和 [核心 CSS](./use.core) 以及 [LESS / SASS](./use.less.sass) 一致。

如果你的项目，支持 LESS / SASS 只需要其自身的**配置**，并不需要任何额外配置，用于支持 SACSS。

唯一注意的是 SACSS 需要 **全局** 引入，不推荐使用 `CSS Module` 的方式。
